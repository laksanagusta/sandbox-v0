# API Configuration
# Base URL for the main API service
VITE_API_BASE_URL=http://localhost:5002

# Base URL for the Identity/Auth API service
VITE_API_IDENTITY_URL=http://localhost:5001

# Note: OpenAI API Key is now configured on the backend (server/.env or root .env)
# See OPENAI_API_KEY in the root .env file
